apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculadora-app.fullname" . }}
  labels:
{{ include "calculadora-app.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "calculadora-app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
