apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calculadora-app.fullname" . }}-test-connection"
  labels:
{{ include "calculadora-app.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "calculadora-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
